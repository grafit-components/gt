import { ItskTreeToggleDirective } from './itsk-tree-toggle.directive';

describe('ItskTreeToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new ItskTreeToggleDirective();
    expect(directive).toBeTruthy();
  });
});

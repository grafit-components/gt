import { ItskTemplateDirective } from './itsk-template.directive';

describe('ItskTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new ItskTemplateDirective(null);
    expect(directive).toBeTruthy();
  });
});

import { ItskMarkDirective } from './itsk-mark.directive';

describe('ItskMarkDirective', () => {
  it('should create an instance', () => {
    const directive = new ItskMarkDirective();
    expect(directive).toBeTruthy();
  });
});

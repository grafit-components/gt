import { ItskTreeSelectOptionDirective } from './itsk-tree-select-option.directive';

describe('ItskSelectOptionDirective', () => {
  it('should create an instance', () => {
    const directive = new ItskTreeSelectOptionDirective();
    expect(directive).toBeTruthy();
  });
});

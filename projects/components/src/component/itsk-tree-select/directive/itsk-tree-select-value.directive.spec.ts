import { ItskTreeSelectValueDirective } from './itsk-tree-select-value.directive';

describe('ItskSelectValueDirective', () => {
  it('should create an instance', () => {
    const directive = new ItskTreeSelectValueDirective();
    expect(directive).toBeTruthy();
  });
});

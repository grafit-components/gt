import { ItskTabContentDirective } from './itsk-tab-content.directive';

describe('ItskTabContentDirective', () => {
  it('should create an instance', () => {
    const directive = new ItskTabContentDirective(null as any);
    expect(directive).toBeTruthy();
  });
});

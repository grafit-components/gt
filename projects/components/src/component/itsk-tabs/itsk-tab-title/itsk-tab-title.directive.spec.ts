import { ItskTabTitleDirective } from './itsk-tab-title.directive';

describe('ItskTabTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new ItskTabTitleDirective();
    expect(directive).toBeTruthy();
  });
});

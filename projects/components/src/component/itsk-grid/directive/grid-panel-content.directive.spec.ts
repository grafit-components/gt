import { GridPanelContentDirective } from './grid-panel-content.directive';

describe('GridPanelContentDirective', () => {
  it('should create an instance', () => {
    const directive = new GridPanelContentDirective();
    expect(directive).toBeTruthy();
  });
});

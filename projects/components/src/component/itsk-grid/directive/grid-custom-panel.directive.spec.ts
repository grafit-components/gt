import { GridCustomPanelDirective } from './grid-custom-panel.directive';

describe('GridPanelCustomDirective', () => {
  it('should create an instance', () => {
    const directive = new GridCustomPanelDirective();
    expect(directive).toBeTruthy();
  });
});

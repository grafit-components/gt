import { GridPanelButtonDirective } from './grid-panel-button.directive';

describe('GridPanelButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new GridPanelButtonDirective();
    expect(directive).toBeTruthy();
  });
});

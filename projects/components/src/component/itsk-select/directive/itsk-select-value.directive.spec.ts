import { ItskSelectValueDirective } from './itsk-select-value.directive';

describe('ItskSelectValueDirective', () => {
  it('should create an instance', () => {
    const directive = new ItskSelectValueDirective();
    expect(directive).toBeTruthy();
  });
});

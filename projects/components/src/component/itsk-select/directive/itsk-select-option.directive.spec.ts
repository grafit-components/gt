import { ItskSelectOptionDirective } from './itsk-select-option.directive';

describe('ItskSelectOptionDirective', () => {
  it('should create an instance', () => {
    const directive = new ItskSelectOptionDirective();
    expect(directive).toBeTruthy();
  });
});

import { ItskMenuItemDirective } from './itsk-menu-item.directive';

describe('ItskMenuItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ItskMenuItemDirective(null as any);
    expect(directive).toBeTruthy();
  });
});

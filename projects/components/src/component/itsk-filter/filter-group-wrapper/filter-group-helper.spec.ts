import { FilterGroupHelper } from './filter-group-helper';

describe('FilterGroupHelper', () => {
  it('should create an instance', () => {
    expect(new FilterGroupHelper()).toBeTruthy();
  });
});

import {ItskFocusDirective} from './itsk-focus.directive';

describe('ItskFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new ItskFocusDirective();
    expect(directive).toBeTruthy();
  });
});

import { ItskSpinnerDirective } from './itsk-spinner.directive';

describe('ItskSpinnerDirective', () => {
  it('should create an instance', () => {
    const directive = new ItskSpinnerDirective();
    expect(directive).toBeTruthy();
  });
});

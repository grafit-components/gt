import { ItskTooltipDirective } from './itsk-tooltip.directive';

describe('ItskTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new ItskTooltipDirective();
    expect(directive).toBeTruthy();
  });
});

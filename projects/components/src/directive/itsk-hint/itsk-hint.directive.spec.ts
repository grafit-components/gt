import {ItskHintDirective} from './itsk-hint.directive';

describe('ItskHintDirective', () => {
  it('should create an instance', () => {
    const directive = new ItskHintDirective();
    expect(directive).toBeTruthy();
  });
});

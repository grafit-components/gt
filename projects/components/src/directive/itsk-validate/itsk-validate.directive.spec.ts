import { ItskValidateDirective } from './itsk-validate.directive';

describe('ItskValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new ItskValidateDirective();
    expect(directive).toBeTruthy();
  });
});

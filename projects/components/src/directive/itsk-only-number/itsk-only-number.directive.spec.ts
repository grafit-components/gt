import { ItskOnlyNumberDirective } from './itsk-only-number.directive';

describe('ItskOnlyNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new ItskOnlyNumberDirective();
    expect(directive).toBeTruthy();
  });
});

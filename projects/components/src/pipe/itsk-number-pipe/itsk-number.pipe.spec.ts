import { ItskNumberPipe } from './itsk-number.pipe';

describe('ItskNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ItskNumberPipe();
    expect(pipe).toBeTruthy();
  });
});

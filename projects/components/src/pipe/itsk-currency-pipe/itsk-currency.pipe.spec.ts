import { ItskCurrencyPipe } from './itsk-currency.pipe';

describe('ItskCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new ItskCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
